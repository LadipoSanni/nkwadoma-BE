<configuration scan="true">
    <!--<configuration debug="true">-->
    <!-- ===== Variables from application.properties ===== -->
    <property scope="context" name="LOG_PATH" value="${log.location:-logs}" />
    <property scope="context" name="LOG_HISTORY_DAYS" value="${log.history:-30}" />
    <!--    <property scope="context" name="LOG_FORMAT" value="${log.format:-text}" />-->
    <property scope="context" name="APP_NAME" value="${log.name:-nkwadoma}" />

    <!-- ===== Pattern Encoder (Text) ===== -->
    <appender name="GENERAL_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${APP_NAME}.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/${APP_NAME}-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>${LOG_HISTORY_DAYS}</maxHistory>
        </rollingPolicy>
    </appender>

    <!-- ===== Pattern Encoder (JSON) ===== -->
    <appender name="GENERAL_FILE_JSON" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${APP_NAME}.log</file>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder"/>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/${APP_NAME}-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>${LOG_HISTORY_DAYS}</maxHistory>
        </rollingPolicy>
    </appender>

    <!-- ===== Error log (only ERROR level) ===== -->
    <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${APP_NAME}-error.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/${APP_NAME}-error-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>${LOG_HISTORY_DAYS}</maxHistory>
        </rollingPolicy>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
    </appender>

    <!-- ===== Console output ===== -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ===== Async Wrappers ===== -->
    <appender name="ASYNC_GENERAL" class="ch.qos.logback.classic.AsyncAppender">
        <queueSize>5000</queueSize>
        <appender-ref ref="GENERAL_FILE" />
        <!--        <appender-ref ref="${LOG_FORMAT.equals('json') ? 'GENERAL_FILE_JSON' : 'GENERAL_FILE'}" />-->
    </appender>

    <appender name="ASYNC_ERROR" class="ch.qos.logback.classic.AsyncAppender">
        <queueSize>2000</queueSize>
        <appender-ref ref="ERROR_FILE" />
    </appender>

    <!-- ===== Optional: Remote Log to Logstash (disabled by default) ===== -->
    <!--
    <appender name="LOGSTASH" class="net.logstash.logback.appender.LogstashTcpSocketAppender">
        <destination>${myapp.remote.log.host}:${myapp.remote.log.port}</destination>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder"/>
    </appender>
    -->

    <!-- ===== Profile-specific root loggers ===== -->
    <springProfile name="local">
        <root level="DEBUG">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="ASYNC_GENERAL"/>
            <appender-ref ref="ASYNC_ERROR"/>
        </root>
    </springProfile>

    <springProfile name="dev,prod,uat">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="ASYNC_GENERAL"/>
            <appender-ref ref="ASYNC_ERROR"/>
            <!-- Uncomment to enable remote logging in prod -->
            <!-- <appender-ref ref="LOGSTASH"/> -->
        </root>
    </springProfile>

</configuration>
